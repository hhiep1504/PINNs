{"metadata":{"kernelspec":{"language":"python","display_name":"Python 3","name":"python3"},"language_info":{"name":"python","version":"3.12.12","mimetype":"text/x-python","codemirror_mode":{"name":"ipython","version":3},"pygments_lexer":"ipython3","nbconvert_exporter":"python","file_extension":".py"},"kaggle":{"accelerator":"nvidiaTeslaT4","dataSources":[],"dockerImageVersionId":31329,"isInternetEnabled":true,"language":"python","sourceType":"notebook","isGpuEnabled":true}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"code","source":"import numpy as np\nimport math\nimport time\nimport torch\nimport torch.nn as nn\nimport torch.optim as optim\nimport torch.autograd as autograd\nimport matplotlib.pyplot as plt\n\n# ==========================================\n# 1. THIẾT LẬP MÔI TRƯỜNG & THÔNG SỐ\n# ==========================================\nSEED = 42\nnp.random.seed(SEED)\ntorch.manual_seed(SEED)\nDEVICE = \"cuda\" if torch.cuda.is_available() else \"cpu\"\nprint(f\"Using device: {DEVICE}\")\n\n# Cấu hình mạng và huấn luyện [7, 9]\nHIDDEN_LAYERS = 3\nHIDDEN_WIDTH = 64\nMAX_EPOCHS = 1000\nPATIENCE   = 20\nMIN_DELTA  = 1e-4\nLR = 1e-3\n\n# Cấu hình UQ và Nhiễu xung [1]\nT_MC = 1000\nP_RANGE = (0.2, 0.5)\nCORRUPTION_RATE = 0.02\nIMPULSE_FACTOR  = 5\n\n# Domain bài toán [3]\nT_MIN, T_MAX = 0.0, 1.0\nX_MIN, X_MAX = -1.0, 1.0\n\nLAMBDA_DATA = 1.0\nLAMBDA_PDE = 1.0\n\n# ==========================================\n# 2. HÀM CHÍNH XÁC, BÀI TOÁN & NHIỄU XUNG\n# ==========================================\ndef exact_solution(t, x):\n    \"\"\" Ground Truth: u(t,x) = e^(-t) * sin(pi*x) \"\"\"\n    return np.exp(-t) * np.sin(np.pi * x)\n\ndef source_term(t, x):\n    \"\"\" f(t,x) = e^(-t)*sin(pi*x) * [-1 + 0.01*pi + pi*e^(-t)*cos(pi*x)] \"\"\"\n    # Tính toán trên GPU [3]\n    pi = math.pi\n    return torch.exp(-t) * torch.sin(pi * x) * (-1.0 + 0.01 * pi + pi * torch.exp(-t) * torch.cos(pi * x))\n\ndef add_impulse_noise(u):\n    \"\"\" Hàm tiêm nhiễu xung vào dữ liệu [1, 5] \"\"\"\n    u_noisy = u.copy()\n    n_total = len(u)\n    if CORRUPTION_RATE > 0:\n        max_y = np.max(np.abs(u))\n        actual_impulse_mag = IMPULSE_FACTOR * max_y\n\n        n_corrupt = int(n_total * CORRUPTION_RATE)\n        corrupt_idx = np.random.choice(n_total, n_corrupt, replace=False)\n        signs = np.random.choice([-1.0, 1.0], size=(n_corrupt, 1))\n        u_noisy[corrupt_idx] = signs * actual_impulse_mag\n    return u_noisy\n\ndef generate_boundary_data_noisy(n_bc=2000):\n    \"\"\" Tạo dữ liệu Biên/IC và áp dụng nhiễu xung [1, 10] \"\"\"\n    # IC: t=0, x in [-1, 1]\n    n_ic = n_bc // 2\n    t_ic = np.zeros((n_ic, 1))\n    x_ic = np.random.uniform(X_MIN, X_MAX, (n_ic, 1))\n    u_ic = exact_solution(t_ic, x_ic)\n\n    # BC Left & Right\n    n_bc_side = n_bc // 4\n    t_bc1 = np.random.uniform(T_MIN, T_MAX, (n_bc_side, 1))\n    x_bc1 = X_MIN * np.ones((n_bc_side, 1))\n    u_bc1 = exact_solution(t_bc1, x_bc1)\n\n    t_bc2 = np.random.uniform(T_MIN, T_MAX, (n_bc_side, 1))\n    x_bc2 = X_MAX * np.ones((n_bc_side, 1))\n    u_bc2 = exact_solution(t_bc2, x_bc2)\n\n    t_data = np.vstack([t_ic, t_bc1, t_bc2])\n    x_data = np.vstack([x_ic, x_bc1, x_bc2])\n\n    TX_bc = np.hstack([t_data, x_data]).astype(np.float32)\n    u_bc_clean = np.vstack([u_ic, u_bc1, u_bc2]).astype(np.float32)\n\n    # Tiêm nhiễu\n    u_bc_noisy = add_impulse_noise(u_bc_clean).astype(np.float32)\n    return TX_bc, u_bc_noisy\n\ndef generate_collocation_points(n_col=1000):\n    t_col = np.random.uniform(T_MIN, T_MAX, (n_col, 1))\n    x_col = np.random.uniform(X_MIN, X_MAX, (n_col, 1))\n    return np.hstack([t_col, x_col]).astype(np.float32)\n\ndef bootstrap(TX, u):\n    n = len(TX)\n    idx = np.random.choice(n, size=n, replace=True)\n    return TX[idx], u[idx]\n\n# ==========================================\n# 3. KIẾN TRÚC MÔ HÌNH & UQ\n# ==========================================\nclass MLP(nn.Module):\n    def __init__(self, in_dim=2, out_dim=1):\n        super().__init__()\n        layers = [nn.Linear(in_dim, HIDDEN_WIDTH), nn.Tanh()]\n        for _ in range(HIDDEN_LAYERS - 1):\n            layers.extend([nn.Linear(HIDDEN_WIDTH, HIDDEN_WIDTH), nn.Tanh()])\n        layers.append(nn.Linear(HIDDEN_WIDTH, out_dim))\n        self.net = nn.Sequential(*layers) # [4] - Đã sửa thành comment\n\n    def forward(self, tx):\n        return self.net(tx)\n\nclass TestTimeBernoulliUQ(nn.Module):\n    def __init__(self, core: nn.Sequential):\n        super().__init__()\n        self.core = core\n        self.p = 0.3\n\n    def set_p(self, p):\n        self.p = float(p)\n\n    def forward(self, tx):\n        out = tx\n        for m in self.core:\n            out = m(out)\n            if isinstance(m, nn.Tanh):\n                msk = (torch.rand_like(out) > self.p).float()\n                out = out * msk / (1.0 - self.p + 1e-12)\n        return out # [11] - Đã sửa thành comment\n\n# ==========================================\n# 4. PDE RESIDUAL, LOSS & ES\n# ==========================================\nmse = nn.MSELoss()\n\ndef pde_residual(model, tx_col):\n    tx_col.requires_grad_(True)\n    u = model(tx_col)\n\n    du = autograd.grad(u, tx_col, torch.ones_like(u), create_graph=True)[0]\n    u_t = du[:, 0:1]\n    u_x = du[:, 1:2]\n    u_xx = autograd.grad(u_x, tx_col, torch.ones_like(u_x), create_graph=True)[0][:, 1:2]\n\n    t = tx_col[:, 0:1]\n    x = tx_col[:, 1:2]\n    f = source_term(t, x)\n\n    res = u_t + u * u_x - (0.01 / math.pi) * u_xx - f\n    return res\n\ndef pinn_loss(model, tx_bc, u_bc, tx_col):\n    u_pred_bc = model(tx_bc)\n    loss_bc = mse(u_pred_bc, u_bc)\n    res = pde_residual(model, tx_col)\n    loss_pde = torch.mean(res**2)\n    return LAMBDA_DATA * loss_bc + LAMBDA_PDE * loss_pde # [11] - Đã sửa thành comment\n\nclass ES:\n    def __init__(self, patience=PATIENCE, min_delta=MIN_DELTA):\n        self.p = patience\n        self.d = min_delta\n        self.best = None\n        self.bad = 0\n\n    def step(self, v):\n        if self.best is None or v < self.best - self.d:\n            self.best = v\n            self.bad = 0\n        else:\n            self.bad += 1\n        return self.bad >= self.p # [11] - Đã sửa thành comment\n\n# ==========================================\n# 5. HUẤN LUYỆN\n# ==========================================\ndef train_member(TX_bc, u_bc, TX_col):\n    model = MLP().to(DEVICE)\n    tx_bc_t = torch.tensor(TX_bc, dtype=torch.float32, device=DEVICE)\n    u_bc_t = torch.tensor(u_bc, dtype=torch.float32, device=DEVICE)\n    tx_col_t = torch.tensor(TX_col, dtype=torch.float32, device=DEVICE)\n\n    opt = optim.Adam(model.parameters(), lr=LR)\n    es = ES(); best_state = None; best_v = float('inf')\n\n    for ep in range(1, MAX_EPOCHS + 1):\n        model.train()\n        opt.zero_grad()\n        loss = pinn_loss(model, tx_bc_t, u_bc_t, tx_col_t)\n        loss.backward()\n        torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)\n        opt.step()\n\n        model.eval()\n        with torch.no_grad():\n            v = float(loss.item())\n            if v < best_v - MIN_DELTA:\n                best_v = v; es.bad = 0\n                best_state = {k: w.detach().cpu().clone() for k, w in model.state_dict().items()}\n            elif es.step(v):\n                break\n\n    if best_state is not None:\n        model.load_state_dict(best_state)\n    return model.eval() # [12] - Đã sửa thành comment\n\n# ==========================================\n# 6. DỰ ĐOÁN & UQ METRICS\n# ==========================================\ndef predict_bagging_det(members, TX_test):\n    \"\"\" Dự báo Deterministic Mean [13] \"\"\"\n    tx_t = torch.tensor(TX_test, dtype=torch.float32, device=DEVICE)\n    with torch.no_grad():\n        preds = [m(tx_t).cpu().numpy().ravel() for m in members]\n    return np.mean(np.stack(preds, 0), axis=0)\n\ndef predict_uq_bagging(members, TX_test):\n    \"\"\" Dự báo UQ (MC Dropout) [13] \"\"\"\n    tx_t = torch.tensor(TX_test, dtype=torch.float32, device=DEVICE)\n    uq_members = [TestTimeBernoulliUQ(m.net).to(DEVICE) for m in members]\n\n    bagged_samples = []\n    for _ in range(T_MC):\n        preds_m = []\n        for uq in uq_members:\n            uq.set_p(np.random.uniform(*P_RANGE))\n            with torch.no_grad():\n                preds_m.append(uq(tx_t).cpu().numpy().ravel())\n        bagged_samples.append(np.mean(np.stack(preds_m, 0), axis=0))\n\n    S = np.stack(bagged_samples, 0)\n    return S.mean(axis=0), S.std(axis=0, ddof=0) # [2, 12] - Đã sửa thành comment\n\ndef picp(mu, sd, y_true):\n    lo, hi = mu - 1.96 * sd, mu + 1.96 * sd\n    yt = y_true.ravel()\n    return float(np.mean((yt >= lo) & (yt <= hi))) * 100 # [2] - Đã sửa thành comment\n\ndef mpiw(sd):\n    return 2 * 1.96 * float(np.mean(sd))\n\n# ==========================================\n# 7. KHẢO SÁT ĐỘ NHẠY\n# ==========================================\ndef main():\n    print(\"=== KHẢO SÁT ĐỘ NHẠY M - BAGGING PINN (VÍ DỤ 3: 1D BURGERS) ===\")\n    print(f\"Cấu hình: Nhiễu xung (Rate={CORRUPTION_RATE*100}%, Biểu độ x{IMPULSE_FACTOR})\\n\")\n\n    # 1. Sinh dữ liệu Training (Chứa Nhiễu) [2]\n    TX_bc_noisy, u_bc_noisy = generate_boundary_data_noisy(n_bc=2000)\n\n    # 2. Tạo lưới Test hoàn toàn sạch để đánh giá \n    nt, nx = 20, 10\n    ts = np.linspace(T_MIN, T_MAX, nt)\n    xs = np.linspace(X_MIN, X_MAX, nx)\n    Tg, Xg = np.meshgrid(ts, xs, indexing='xy')\n    TX_test = np.c_[Tg.ravel(), Xg.ravel()].astype(np.float32)\n    u_exact_test = exact_solution(TX_test[:, 0], TX_test[:, 1])\n\n    # 3. Phân tích độ nhạy với M tăng dần\n    M_LIST = [1,2,5,10,20,50,100,200] # Đã sửa typo\n    bagging_members = []\n    total_time = 0.0\n\n    print(f\"{'M':<5} | {'Thời gian(s)':<13} | {'MAE (Det)':<10} | {'RMSE (Det)':<11} | {'PICP (%)':<10} | {'MPIW'}\")\n    print(\"-\" * 80)\n\n    for target_m in M_LIST:\n        start_time = time.time()\n\n        # Huấn luyện cộng dồn các mô hình còn thiếu [2]\n        while len(bagging_members) < target_m:\n            TX_bc_b, u_bc_b = bootstrap(TX_bc_noisy, u_bc_noisy)\n            TX_col_b = generate_collocation_points(n_col=5000)\n\n            trained_model = train_member(TX_bc_b, u_bc_b, TX_col_b)\n            bagging_members.append(trained_model)\n\n        total_time += (time.time() - start_time)\n\n        # Đánh giá Deterministic\n        y_pred_det = predict_bagging_det(bagging_members, TX_test)\n        mae_det = float(np.mean(np.abs(y_pred_det - u_exact_test)))\n        rmse_det = float(np.sqrt(np.mean((y_pred_det - u_exact_test)**2)))\n\n        # Đánh giá Uncertainty Quantification (UQ)\n        y_mean_uq, y_sd_uq = predict_uq_bagging(bagging_members, TX_test)\n        cr_val = picp(y_mean_uq, y_sd_uq, u_exact_test)\n        mpiw_val = mpiw(y_sd_uq)\n\n        print(f\"{target_m:<5} | {total_time:<13.2f} | {mae_det:<10.6f} | {rmse_det:<11.6f} | {cr_val:>6.2f}     | {mpiw_val:.6f}\") # [2] - Đã sửa thành comment\n\nif __name__ == \"__main__\":\n    main()","metadata":{"_uuid":"8f2839f25d086af736a60e9eeb907d3b93b6e0e5","_cell_guid":"b1076dfc-b9ad-4769-8c92-a6c4dae69d19","trusted":true,"execution":{"iopub.status.busy":"2026-04-01T17:33:29.931780Z","iopub.execute_input":"2026-04-01T17:33:29.932399Z","iopub.status.idle":"2026-04-01T17:43:47.162410Z","shell.execute_reply.started":"2026-04-01T17:33:29.932369Z","shell.execute_reply":"2026-04-01T17:43:47.161632Z"}},"outputs":[{"name":"stdout","text":"Using device: cuda\n=== KHẢO SÁT ĐỘ NHẠY M - BAGGING PINN (VÍ DỤ 3: 1D BURGERS) ===\nCấu hình: Nhiễu xung (Rate=2.0%, Biểu độ x5)\n\nM     | Thời gian(s)  | MAE (Det)  | RMSE (Det)  | PICP (%)   | MPIW\n--------------------------------------------------------------------------------\n1     | 2.50          | 0.026575   | 0.034432    | 100.00     | 4.562630\n2     | 4.54          | 0.021773   | 0.029067    | 100.00     | 3.470880\n5     | 11.54         | 0.018458   | 0.025717    | 100.00     | 2.236662\n10    | 23.96         | 0.023029   | 0.029796    | 100.00     | 1.597462\n20    | 45.01         | 0.020660   | 0.026979    | 100.00     | 1.114191\n50    | 109.27        | 0.017247   | 0.022930    | 100.00     | 0.691762\n100   | 211.44        | 0.018610   | 0.024652    | 100.00     | 0.492102\n200   | 418.98        | 0.019344   | 0.025440    | 100.00     | 0.347116\n","output_type":"stream"}],"execution_count":6},{"cell_type":"code","source":"import numpy as np, random, math\nimport torch, torch.nn as nn\nimport torch.optim as optim\nimport torch.autograd as autograd\nimport matplotlib.pyplot as plt\nfrom mpl_toolkits.mplot3d import Axes3D  # noqa: F401\nfrom matplotlib import cm","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-01T17:31:19.184146Z","iopub.execute_input":"2026-04-01T17:31:19.184758Z","iopub.status.idle":"2026-04-01T17:31:19.188356Z","shell.execute_reply.started":"2026-04-01T17:31:19.184730Z","shell.execute_reply":"2026-04-01T17:31:19.187700Z"}},"outputs":[],"execution_count":4},{"cell_type":"code","source":"SEED = 42\nrandom.seed(SEED)\nnp.random.seed(SEED)\ntorch.manual_seed(SEED)\nDEVICE = \"cuda\" if torch.cuda.is_available() else \"cpu\"\nprint(f\"Using device: {DEVICE}\")\n\n# Cấu hình kiến trúc mạng và huấn luyện\nHIDDEN_LAYERS = 3\nHIDDEN_WIDTH = 64\nMAX_EPOCHS = 1000\nPATIENCE   = 20\nMIN_DELTA  = 1e-4\nLR = 1e-3\n\n# Cấu hình Ensemble và UQ\nM_BAG = 50        \nT_MC = 1000         \nP_RANGE = (0.2, 0.5)\n\n# Cấu hình Nhiễu xung (Impulse Noise)\nCORRUPTION_RATE = 0.0  # \nIMPULSE_FACTOR  = 0  # Biên độ x2 so với max(|u|)\n\n# Domain bài toán EX3 (1D Burgers)\nT_MIN, T_MAX = 0.0, 1.0\nX_MIN, X_MAX = -1.0, 1.0\n\nLAMBDA_DATA = 1.0  \nLAMBDA_PDE = 1\n\n# ==========================================\n# 2. HÀM CHÍNH XÁC & NHIỄU XUNG\n# ==========================================\ndef exact_solution(t, x):\n    \"\"\" Ground Truth: u(t,x) = e^(-t) * sin(pi*x) \"\"\"\n    return np.exp(-t) * np.sin(np.pi * x)\n\ndef source_term(t, x):\n    \"\"\" f(t,x) = e^(-t)*sin(pi*x) * [-1 + 0.01*pi + pi*e^(-t)*cos(pi*x)] \"\"\"\n    pi = math.pi\n    return torch.exp(-t) * torch.sin(pi * x) * (-1.0 + 0.01 * pi + pi * torch.exp(-t) * torch.cos(pi * x))\n\ndef add_impulse_noise(u):\n    \"\"\" Hàm tiêm nhiễu xung (Salt & Pepper) \"\"\"\n    u_noisy = u.copy()\n    n_total = len(u)\n    if CORRUPTION_RATE > 0:\n        max_y = np.max(np.abs(u))\n        actual_impulse_mag = IMPULSE_FACTOR * max_y\n        n_corrupt = int(n_total * CORRUPTION_RATE)\n        corrupt_idx = np.random.choice(n_total, n_corrupt, replace=False)\n        signs = np.random.choice([-1.0, 1.0], size=(n_corrupt, 1))\n        u_noisy[corrupt_idx] = signs * actual_impulse_mag\n    return u_noisy\n\ndef generate_supervised_data_noisy(n_bc=400):\n    \"\"\" Tạo dữ liệu Initial Conditions và Boundary Conditions (Có Nhiễu) \"\"\"\n    n_ic = n_bc // 2\n    t_ic = np.zeros((n_ic, 1))\n    x_ic = np.random.uniform(X_MIN, X_MAX, (n_ic, 1))\n    u_ic = exact_solution(t_ic, x_ic)\n    \n    n_bc_side = n_bc // 4\n    t_bc1 = np.random.uniform(T_MIN, T_MAX, (n_bc_side, 1))\n    x_bc1 = X_MIN * np.ones((n_bc_side, 1))\n    u_bc1 = exact_solution(t_bc1, x_bc1)\n    \n    t_bc2 = np.random.uniform(T_MIN, T_MAX, (n_bc_side, 1))\n    x_bc2 = X_MAX * np.ones((n_bc_side, 1))\n    u_bc2 = exact_solution(t_bc2, x_bc2)\n    \n    t_data = np.vstack([t_ic, t_bc1, t_bc2])\n    x_data = np.vstack([x_ic, x_bc1, x_bc2])\n    \n    TX_bc = np.hstack([t_data, x_data]).astype(np.float32)\n    u_bc_clean = np.vstack([u_ic, u_bc1, u_bc2]).astype(np.float32)\n    \n    u_bc_noisy = add_impulse_noise(u_bc_clean)\n    return TX_bc, u_bc_noisy\n\ndef generate_clean_test_grid(nt=60, nx=60):\n    \"\"\" Lưới test hoàn toàn sạch để đánh giá \"\"\"\n    ts = np.linspace(T_MIN, T_MAX, nt)\n    xs = np.linspace(X_MIN, X_MAX, nx)\n    Tg, Xg = np.meshgrid(ts, xs, indexing='xy')\n    TX_test = np.c_[Tg.ravel(), Xg.ravel()].astype(np.float32)\n    u_exact = exact_solution(TX_test[:, 0], TX_test[:, 1]).ravel()\n    return TX_test, u_exact\n\ndef generate_collocation_points(n_col=1000):\n    t_col = np.random.uniform(T_MIN, T_MAX, (n_col, 1))\n    x_col = np.random.uniform(X_MIN, X_MAX, (n_col, 1))\n    return np.hstack([t_col, x_col]).astype(np.float32)\n\ndef bootstrap(TX, u):\n    n = len(TX)\n    idx = np.random.choice(n, size=n, replace=True)\n    return TX[idx], u[idx]\n\n# ==========================================\n# 3. KIẾN TRÚC MÔ HÌNH\n# ==========================================\nclass MLP(nn.Module):\n    def __init__(self, in_dim=2, out_dim=1):\n        super().__init__()\n        layers = [nn.Linear(in_dim, HIDDEN_WIDTH), nn.Tanh()]\n        for _ in range(HIDDEN_LAYERS - 1):\n            layers.extend([nn.Linear(HIDDEN_WIDTH, HIDDEN_WIDTH), nn.Tanh()])\n        layers.append(nn.Linear(HIDDEN_WIDTH, out_dim))\n        self.net = nn.Sequential(*layers)\n\n    def forward(self, tx):\n        return self.net(tx)\n\nclass TestTimeBernoulliUQ(nn.Module):\n    def __init__(self, core: nn.Sequential):\n        super().__init__()\n        self.core = core\n        self.p = 0.3\n\n    def set_p(self, p):\n        self.p = float(p)\n\n    def forward(self, tx):\n        out = tx\n        for m in self.core:\n            out = m(out)\n            if isinstance(m, nn.Tanh):\n                msk = (torch.rand_like(out) > self.p).float()\n                out = out * msk / (1.0 - self.p + 1e-12)\n        return out\n\n# ==========================================\n# 4. PDE RESIDUAL & LOSSES\n# ==========================================\nmse = nn.MSELoss()\n\ndef ann_loss(model, tx, u): \n    return mse(model(tx), u)\n\ndef pde_residual(model, tx_col):\n    tx_col.requires_grad_(True)\n    u = model(tx_col)\n    \n    du = autograd.grad(u, tx_col, torch.ones_like(u), create_graph=True)[0]\n    u_t = du[:, 0:1]\n    u_x = du[:, 1:2]\n    u_xx = autograd.grad(u_x, tx_col, torch.ones_like(u_x), create_graph=True)[0][:, 1:2]\n    \n    t = tx_col[:, 0:1]\n    x = tx_col[:, 1:2]\n    f = source_term(t, x)\n    \n    return u_t + u * u_x - (0.01 / math.pi) * u_xx - f\n\ndef pinn_total_loss(model, tx_sup, u_sup, tx_col):\n    l_data = mse(model(tx_sup), u_sup)\n    res = pde_residual(model, tx_col)\n    l_pde = torch.mean(res**2)\n    return LAMBDA_DATA * l_data + LAMBDA_PDE * l_pde\n\nclass ES:\n    def __init__(self, patience=PATIENCE, min_delta=MIN_DELTA):\n        self.p = patience; self.d = min_delta; self.best = None; self.bad = 0\n    def step(self, v):\n        if self.best is None or v < self.best - self.d:\n            self.best = v; self.bad = 0\n        else:\n            self.bad += 1\n        return self.bad >= self.p\n\n# ==========================================\n# 5. TRAINING FUNCTIONS\n# ==========================================\ndef train_ann(Xsup, ysup):\n    model = MLP().to(DEVICE)\n    xt_tr = torch.tensor(Xsup, dtype=torch.float32, device=DEVICE)\n    u_tr  = torch.tensor(ysup, dtype=torch.float32, device=DEVICE)\n    opt = optim.Adam(model.parameters(), lr=LR)\n    es = ES(); best, bestv = None, float('inf')\n\n    for ep in range(1, MAX_EPOCHS + 1):\n        model.train(); opt.zero_grad()\n        loss = ann_loss(model, xt_tr, u_tr)\n        loss.backward(); opt.step()\n\n        model.eval()\n        with torch.no_grad():\n            v = float(loss.item())\n            if v < bestv - MIN_DELTA:\n                bestv = v; es.bad = 0; best = {k:w.detach().cpu().clone() for k,w in model.state_dict().items()}\n            elif es.step(v): break\n    if best is not None: model.load_state_dict(best)\n    return model.eval()\n\ndef train_pinn(Xsup, ysup, Xcol):\n    model = MLP().to(DEVICE)\n    xt_tr = torch.tensor(Xsup, dtype=torch.float32, device=DEVICE)\n    u_tr  = torch.tensor(ysup, dtype=torch.float32, device=DEVICE)\n    xt_c  = torch.tensor(Xcol, dtype=torch.float32, device=DEVICE)\n    opt = optim.Adam(model.parameters(), lr=LR)\n    es = ES(); best, bestv = None, float('inf')\n\n    for ep in range(1, MAX_EPOCHS + 1):\n        model.train(); opt.zero_grad()\n        loss = pinn_total_loss(model, xt_tr, u_tr, xt_c)\n        loss.backward()\n        torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)\n        opt.step()\n\n        model.eval()\n        with torch.no_grad():\n            v = float(loss.item())\n            if v < bestv - MIN_DELTA:\n                bestv = v; es.bad = 0; best = {k:w.detach().cpu().clone() for k,w in model.state_dict().items()}\n            elif es.step(v): break\n    if best is not None: model.load_state_dict(best)\n    return model.eval()\n\n# ==========================================\n# 6. PREDICT & METRICS\n# ==========================================\n@torch.no_grad()\ndef predict_det_single(model, X):\n    xt = torch.tensor(X, dtype=torch.float32, device=DEVICE)\n    return model(xt).cpu().numpy().ravel()\n\n@torch.no_grad()\ndef predict_det_bagging(members, X):\n    preds = [predict_det_single(m, X) for m in members]\n    return np.mean(np.stack(preds, 0), 0)\n\ndef mae_np(yhat, ytrue): return float(np.mean(np.abs(yhat - ytrue.ravel())))\ndef mse_np(yhat, ytrue): return float(np.mean((yhat - ytrue.ravel())**2))\ndef rmse_np(yhat, ytrue): return float(np.sqrt(mse_np(yhat, ytrue)))\n\ndef predict_uq_single(model, X_np):\n    Xt = torch.tensor(X_np, dtype=torch.float32, device=DEVICE)\n    uq = TestTimeBernoulliUQ(model.net).to(DEVICE)\n    samples = []\n    for _ in range(T_MC):\n        uq.set_p(random.uniform(*P_RANGE))\n        with torch.no_grad():\n            samples.append(uq(Xt).cpu().numpy().ravel())\n    S = np.stack(samples, 0)\n    return S.mean(0), S.std(0, ddof=0)\n\ndef predict_uq_bagging(members, X_np):\n    Xt = torch.tensor(X_np, dtype=torch.float32, device=DEVICE)\n    uq_members = [TestTimeBernoulliUQ(m.net).to(DEVICE) for m in members]\n    bagged = []\n    for _ in range(T_MC):\n        preds_m = []\n        for uq in uq_members:\n            uq.set_p(random.uniform(*P_RANGE))\n            with torch.no_grad():\n                preds_m.append(uq(Xt).cpu().numpy().ravel())\n        bagged.append(np.mean(np.stack(preds_m, 0), 0))\n    S = np.stack(bagged, 0)\n    return S.mean(0), S.std(0, ddof=0)\n\ndef picp(mu, sd, y_true):\n    lo, hi = mu - 1.96 * sd, mu + 1.96 * sd\n    yt = y_true.ravel()\n    return float(np.mean((yt >= lo) & (yt <= hi))) * 100\n\ndef mpiw(sd):\n    return 2 * 1.96 * float(np.mean(sd))\n\n# ==========================================\n# 7. MAIN EXECUTION\n# ==========================================\ndef main():\n    print(f\"=== SO SÁNH 4 MÔ HÌNH (EX3) | IMPULSE NOISE: {int(CORRUPTION_RATE*100)}%, FACTOR: {IMPULSE_FACTOR}x ===\")\n    \n    # 0. Tạo dữ liệu dùng chung (Có nhiễu xung)\n    TX_sup, u_sup = generate_supervised_data_noisy(n_bc=2000)\n    \n    # Lưới test hoàn toàn sạch để đánh giá \n    TX_test, u_exact = generate_clean_test_grid()\n    \n    # ==========================================\n    # 1. Standard ANN (Không học PDE)\n    # ==========================================\n    print(\"\\n[1/4] Training Standard ANN...\")\n    trained_ann = train_ann(TX_sup, u_sup)\n    y_pred_ann = predict_det_single(trained_ann, TX_test)\n    y_mean_ann, y_std_ann = predict_uq_single(trained_ann, TX_test)\n    \n    # ==========================================\n    # 2. Standard PINN (Dữ liệu + PDE)\n    # ==========================================\n    print(\"[2/4] Training Standard PINN...\")\n    TX_col_pinn = generate_collocation_points(n_col=5000)\n    trained_pinn = train_pinn(TX_sup, u_sup, TX_col_pinn)\n    y_pred_pinn = predict_det_single(trained_pinn, TX_test)\n    y_mean_pinn, y_std_pinn = predict_uq_single(trained_pinn, TX_test)\n    \n    # ==========================================\n    # 3. Bagging PINN (Bootstrap + Random Collocation)\n    # ==========================================\n    print(\"[3/4] Training Bagging PINN...\")\n    bagging_members = []\n    for m in range(M_BAG):\n        TX_b, u_b = bootstrap(TX_sup, u_sup)\n        TX_col_b = generate_collocation_points(n_col=5000) # Mỗi model học không gian PDE hơi khác nhau\n        bagging_members.append(train_pinn(TX_b, u_b, TX_col_b))\n        \n    y_pred_bag = predict_det_bagging(bagging_members, TX_test)\n    y_mean_bag, y_std_bag = predict_uq_bagging(bagging_members, TX_test)\n    \n    # ==========================================\n    # 4. Initialization-Only PINN (No Bootstrap)\n    # ==========================================\n    print(\"[4/4] Training Initialization-Only Ensemble PINN...\")\n    init_members = []\n    TX_col_shared = generate_collocation_points(n_col=5000) # Dùng chung điểm PDE\n    for m in range(M_BAG):\n        init_members.append(train_pinn(TX_sup, u_sup, TX_col_shared))\n        \n    y_pred_init = predict_det_bagging(init_members, TX_test)\n    y_mean_init, y_std_init = predict_uq_bagging(init_members, TX_test)\n    \n    # ==========================================\n    # In Kết quả Tổng hợp\n    # ==========================================\n    print(\"\\n\" + \"=\"*115)\n    print(f\"{'MÔ HÌNH':<25} | {'MAE':<8} | {'MSE':<8} | {'RMSE':<8} | {'Mean Std':<10} | {'PICP (%)':<10} | {'MPIW'}\")\n    print(\"=\"*115)\n\n    print(f\"{'ANN (Det)':<25} | {mae_np(y_pred_ann, u_exact):.4f}   | {mse_np(y_pred_ann, u_exact):.4f}   | {rmse_np(y_pred_ann, u_exact):.4f}   | {'-':<10} | {'-':<10} | -\")\n    print(f\"{'ANN (UQ Mean)':<25} | {mae_np(y_mean_ann, u_exact):.4f}   | {mse_np(y_mean_ann, u_exact):.4f}   | {rmse_np(y_mean_ann, u_exact):.4f}   | {np.mean(y_std_ann):<10.4f} | {picp(y_mean_ann, y_std_ann, u_exact):<10.2f} | {mpiw(y_std_ann):.4f}\")\n    print(\"-\" * 115)\n\n    print(f\"{'PINN (Det)':<25} | {mae_np(y_pred_pinn, u_exact):.4f}   | {mse_np(y_pred_pinn, u_exact):.4f}   | {rmse_np(y_pred_pinn, u_exact):.4f}   | {'-':<10} | {'-':<10} | -\")\n    print(f\"{'PINN (UQ Mean)':<25} | {mae_np(y_mean_pinn, u_exact):.4f}   | {mse_np(y_mean_pinn, u_exact):.4f}   | {rmse_np(y_mean_pinn, u_exact):.4f}   | {np.mean(y_std_pinn):<10.4f} | {picp(y_mean_pinn, y_std_pinn, u_exact):<10.2f} | {mpiw(y_std_pinn):.4f}\")\n    print(\"-\" * 115)\n\n    print(f\"{'Bagging PINN (Det)':<25} | {mae_np(y_pred_bag, u_exact):.4f}   | {mse_np(y_pred_bag, u_exact):.4f}   | {rmse_np(y_pred_bag, u_exact):.4f}   | {'-':<10} | {'-':<10} | -\")\n    print(f\"{'Bagging PINN (UQ Mean)':<25} | {mae_np(y_mean_bag, u_exact):.4f}   | {mse_np(y_mean_bag, u_exact):.4f}   | {rmse_np(y_mean_bag, u_exact):.4f}   | {np.mean(y_std_bag):<10.4f} | {picp(y_mean_bag, y_std_bag, u_exact):<10.2f} | {mpiw(y_std_bag):.4f}\")\n    print(\"-\" * 115)\n\n    print(f\"{'Init-Only PINN (Det)':<25} | {mae_np(y_pred_init, u_exact):.4f}   | {mse_np(y_pred_init, u_exact):.4f}   | {rmse_np(y_pred_init, u_exact):.4f}   | {'-':<10} | {'-':<10} | -\")\n    print(f\"{'Init-Only PINN (UQ)':<25} | {mae_np(y_mean_init, u_exact):.4f}   | {mse_np(y_mean_init, u_exact):.4f}   | {rmse_np(y_mean_init, u_exact):.4f}   | {np.mean(y_std_init):<10.4f} | {picp(y_mean_init, y_std_init, u_exact):<10.2f} | {mpiw(y_std_init):.4f}\")\n    print(\"=\"*115)\n\nif __name__ == \"__main__\":\n    main()","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-01T17:55:00.340588Z","iopub.execute_input":"2026-04-01T17:55:00.341170Z","iopub.status.idle":"2026-04-01T17:59:20.888442Z","shell.execute_reply.started":"2026-04-01T17:55:00.341140Z","shell.execute_reply":"2026-04-01T17:59:20.887547Z"}},"outputs":[{"name":"stdout","text":"Using device: cuda\n=== SO SÁNH 4 MÔ HÌNH (EX3) | IMPULSE NOISE: 0%, FACTOR: 0x ===\n\n[1/4] Training Standard ANN...\n[2/4] Training Standard PINN...\n[3/4] Training Bagging PINN...\n[4/4] Training Initialization-Only Ensemble PINN...\n\n===================================================================================================================\nMÔ HÌNH                   | MAE      | MSE      | RMSE     | Mean Std   | PICP (%)   | MPIW\n===================================================================================================================\nANN (Det)                 | 0.1485   | 0.0423   | 0.2057   | -          | -          | -\nANN (UQ Mean)             | 0.1455   | 0.0358   | 0.1893   | 1.0403     | 100.00     | 4.0780\n-------------------------------------------------------------------------------------------------------------------\nPINN (Det)                | 0.0287   | 0.0011   | 0.0330   | -          | -          | -\nPINN (UQ Mean)            | 0.0467   | 0.0034   | 0.0583   | 1.3253     | 100.00     | 5.1950\n-------------------------------------------------------------------------------------------------------------------\nBagging PINN (Det)        | 0.0143   | 0.0003   | 0.0177   | -          | -          | -\nBagging PINN (UQ Mean)    | 0.0321   | 0.0014   | 0.0375   | 0.1731     | 100.00     | 0.6786\n-------------------------------------------------------------------------------------------------------------------\nInit-Only PINN (Det)      | 0.0159   | 0.0004   | 0.0196   | -          | -          | -\nInit-Only PINN (UQ)       | 0.0332   | 0.0015   | 0.0385   | 0.1741     | 100.00     | 0.6826\n===================================================================================================================\n","output_type":"stream"}],"execution_count":8}]}